apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ai-cicd-nodeapp
  namespace: ai-cicd
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ai-cicd-nodeapp
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
